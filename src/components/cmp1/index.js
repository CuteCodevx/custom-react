import React from '../../React';

class FirstChildCmp extends React.Component {
  constructor(props) {
    super(props);
  }
  render() {
    return <div>我是第一个child</div>;
  }
}

export default FirstChildCmp;
